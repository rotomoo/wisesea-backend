package com.example.knu.domain.repository;

import com.example.knu.domain.entity.BoardPostHashtag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardPostHashtagRepository extends JpaRepository<BoardPostHashtag, Long> {
}
